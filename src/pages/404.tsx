export const NotFoundPage = ({}: NotFoundPageProps) => {
  return (
    <div className="max-w-contained flex h-dvh items-center justify-center">
      <p className="text-medium mb-4 text-lg">Not found</p>;
    </div>
  );
};

type NotFoundPageProps = {};
